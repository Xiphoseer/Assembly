NameValue
=========

In many places within the game files, there is a format called `LwoNameValue`
internally, which acts as a typed name-value store. This is reflected in
assembly via the following classes:

Typedefs
--------

.. doxygentypedef:: assembly::namevalue::name_value_store
